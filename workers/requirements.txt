# VisionCut AI — Python workers
# Pin minimum versions; lock the rest in a separate constraints.txt in CI.

# Web framework
fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.1
python-multipart==0.0.20

# HTTP / queue
httpx==0.27.2
redis==5.2.0
bullmq==2.9.4

# Database
asyncpg==0.30.0
psycopg2-binary==2.9.10
sqlalchemy[asyncio]==2.0.36
pgvector==0.3.6

# AI / ML
google-generativeai==0.8.3
openai==1.55.0
openai-whisper @ git+https://github.com/openai/whisper.git
faster-whisper==1.0.3         # GPU-accelerated Whisper via CTranslate2
ultralytics==8.3.21
torch==2.5.0
torchvision==0.20.0
transformers==4.41.2  # 4.42+ ships a deeply-nested deprecated/trajectory_transformer module that breaks on Win + pip without LongPathsEnabled

# CV / audio
opencv-python-headless==4.10.0.84
librosa==0.10.2
soundfile==0.12.1
numpy==1.26.4
pillow==11.0.0

# FAISS — install faiss-gpu on hosts with CUDA; faiss-cpu is the safe default.
# On a CUDA host:  pip install faiss-gpu==1.7.2.post1
# (We don't pin faiss-gpu in requirements.txt because it requires CUDA runtime libs.)
faiss-cpu==1.9.0

# FFmpeg wrapper
ffmpeg-python==0.2.0

# Perceptual quality
scikit-image==0.24.0
scipy==1.14.1

# Reliability
tenacity==9.0.0
structlog==24.4.0
sentry-sdk==2.18.0

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
